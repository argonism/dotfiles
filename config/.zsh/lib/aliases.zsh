# .zshrc reload
alias re="source ~/.zshrc"

# cd by dir name only for ..
alias ..="cd .."

# git
alias gs='git status'
alias gdc='git diff --cached'

# python
alias u='UV_ENV_FILE=".env" uv run'

# misc
alias ll='ls -laGh'
